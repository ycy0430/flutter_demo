import 'package:flutter/material.dart';
import 'package:html/parser.dart' show parse;
import 'package:html/dom.dart'  show dom;

class TextPage1 extends StatelessWidget {
  const TextPage1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Contetnt(),
      
    );
  }
}

class Contetnt extends StatelessWidget {

  
  Widget build(BuildContext context) {
    return Center(
    );
  }
}